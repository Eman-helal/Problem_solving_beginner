#include <iostream>

using namespace std;

int main()
{
	// To calculate the sum From 1 to a number it is calculated simply by the formula (n(n+1))/2
	int N;
	//Taking a number from the user
	cin >> N;
	//Printing the output
	cout << (N * (N + 1)) / 2;
}
