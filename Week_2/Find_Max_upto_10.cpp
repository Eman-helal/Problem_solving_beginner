#include <iostream>

using namespace std;

int main()
{
	int n, max, num; 
	cin >> n >> max;
	n--;
	if (n > 0) {
		cin >> num;
		if (max < num)
			max = num;
		--n;
	}
	if (n > 0) {
		cin >> num;
		if (max < num)
			max = num;
		--n;
	}
	if (n > 0) {
		cin >> num;
		if (max < num)
			max = num;
		--n;
	}
	if (n > 0) {
		cin >> num;
		if (max < num)
			max = num;
		--n;
	}
	if (n > 0) {
		cin >> num;
		if (max < num)
			max = num;
		--n;
	}
	if (n > 0) {
		cin >> num;
		if (max < num)
			max = num;
		--n;
	}
	if (n > 0) {
		cin >> num;
		if (max < num)
			max = num;
		--n;
	}
	if (n > 0) {
		cin >> num;
		if (max < num)
			max = num;
		--n;
	}
	if (n > 0) {
		cin >> num;
		if (max < num)
			max = num;
		--n;
	}
	if (n > 0) {
		cin >> num;
		if (max < num)
			max = num;
		--n;
	}
	cout << max << endl;
}
