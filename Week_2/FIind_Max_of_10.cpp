#include <iostream>

using namespace std;

int main()
{
	int n, max;
	cin >> max >> n;
	if (n > max)
		max = n;
	cin >> n;
	if (n > max)
		max = n;
	cin >> n;
	if (n > max)
		max = n;
	cin >> n;
	if (n > max)
		max = n;
	cin >> n;
	if (n > max)
		max = n;
	cin >> n;
	if (n > max)
		max = n;
	cin >> n;
	if (n > max)
		max = n;
	cin >> n;
	if (n > max)
		max = n;
	cin >> n;
	if (n > max)
		max = n;
	cout << max << endl;
}
